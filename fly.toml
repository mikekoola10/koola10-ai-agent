# fly.toml app configuration file generated for koola10 on 2026-05-17T23:09:30-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'koola10'
primary_region = 'ams'

[env]
  PORT = '8080'

[processes]
  app = './agent'

[[mounts]]
  source = 'metaclaw_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '10s'
    timeout = '2s'
    grace_period = '5s'
    method = 'get'
    path = '/'
