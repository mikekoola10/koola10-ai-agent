{
  "name": "koola10-nova-agent",
  "version": "0.4.0",
  "description": "Nova — a Manus-style autonomous AI agent with shared tool integrations.",
  "type": "module",
  "license": "MIT",
  "author": "mikekoola10",
  "main": "dist/index.js",
  "bin": {
    "nova": "./dist/index.js",
    "nova-ui": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "ui": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "smoke": "node dist/index.js --mock \"explore the current directory and write a summary report\"",
    "tools:browser": "npx playwright install chromium",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@composio/core": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "playwright": "^1.49.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
